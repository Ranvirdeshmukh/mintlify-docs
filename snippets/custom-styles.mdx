:root {
  /* Primary accent - deeper blue for a more formal, enterprise feel */
  --primary-color: #1e40af;
  --primary-hover: #1e3a8a;
  --primary-light: rgba(30, 64, 175, 0.1);
  
  /* Neutral tones */
  --white: #ffffff;
  --gray-50: #f9f9f9;
  --gray-100: #f3f3f3;
  --gray-200: #e6e6e6;
  --gray-300: #d1d1d1;
  --gray-500: #8e8e8e;
  --gray-700: #636363;
  --gray-900: #333333;
  
  /* Status colors (more subtle) */
  --success: rgb(52, 199, 89);
  --warning: rgb(255, 149, 0);
  --error: rgb(255, 59, 48);
  --success-light: rgba(52, 199, 89, 0.1);
  --warning-light: rgba(255, 149, 0, 0.1);
  --error-light: rgba(255, 59, 48, 0.1);

  /* Font styles */
  --font-family: "'SF Pro Display', -apple-system, BlinkMacSystemFont, sans-serif";
  --letter-spacing-heading: -0.03em;
  --letter-spacing-body: -0.01em;
}

/* Apply global font family */
body {
  font-family: var(--font-family);
  color: var(--gray-900);
  letter-spacing: var(--letter-spacing-body);
}

/* Headings */
h1, h2, h3, h4, h5, h6 {
  font-weight: 600;
  letter-spacing: var(--letter-spacing-heading);
}

/* Links */
a {
  color: var(--primary-color);
  transition: color 0.2s ease;
}

a:hover {
  color: var(--primary-hover);
}

/* Buttons */
button, .button {
  font-family: var(--font-family);
  font-weight: 500;
}

.primary-button {
  background: linear-gradient(to right, var(--primary-color), #3b82f6);
  color: var(--white);
  box-shadow: 0 4px 14px rgba(30, 64, 175, 0.25), 0 8px 30px rgba(30, 64, 175, 0.15);
}

.primary-button:hover {
  box-shadow: 0 6px 20px rgba(37, 99, 235, 0.3), 0 12px 28px rgba(37, 99, 235, 0.2);
}

/* Status indicators */
.success {
  color: var(--success);
  background-color: var(--success-light);
}

.warning {
  color: var(--warning);
  background-color: var(--warning-light);
}

.error {
  color: var(--error);
  background-color: var(--error-light);
}

/* Cards and containers */
.card {
  border-radius: 12px;
  border: 1px solid var(--gray-200);
  background-color: var(--white);
} 